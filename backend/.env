# Application Settings
APP_NAME="AI Medical Assistant"
DEBUG=true

# Database Configuration
DATABASE_TYPE=sqlite
DATABASE_URL=sqlite+aiosqlite:///./med_assistant.db
# For PostgreSQL migration:
# DATABASE_TYPE=postgresql
# POSTGRES_URL=postgresql+asyncpg://user:password@localhost:5432/med_assistant

# Security - CHANGE THESE IN PRODUCTION
SECRET_KEY=your-super-secret-key-change-in-production
ENCRYPTION_KEY=dzufYl59IM5-7TybTQ6f_4jeGfUN4hoXU7tQd782bUU=

# LLM Configuration
# Options: "gcp" for Google Cloud MedGemma, "local" for local LLM (Ollama, etc.)
LLM_PROVIDER=local

# GCP MedGemma / Vertex AI Settings (if LLM_PROVIDER=gcp)
GCP_PROJECT_ID=977337325460
GCP_LOCATION=us-central1
GCP_ENDPOINT_ID=mg-endpoint-1f3addd2-8e87-4fcd-a6e2-019ff94fce3e
GCP_API_ENDPOINT=us-central1-aiplatform.googleapis.com
GCP_DEDICATED_ENDPOINT=
# Note: Authentication uses Application Default Credentials (ADC)
# Run: gcloud auth application-default login

# Local LLM Settings (if LLM_PROVIDER=local)
LOCAL_LLM_URL=http://localhost:11434
LOCAL_LLM_MODEL=llama3.2

# HIPAA Audit Logging
AUDIT_LOG_ENABLED=true
AUDIT_LOG_PATH=./logs/audit.log

# Family Member Limit
MAX_FAMILY_MEMBERS=6
